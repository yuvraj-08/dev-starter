"use client";

import React, { ReactNode } from "react";

export function ThemeProvider({ children }: { children: ReactNode }) {
  // You can add theme context/provider logic here later
  return <>{children}</>;
}
